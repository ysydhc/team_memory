---
description: Plan 执行门控与记录
globs:
  - "**/plan*.md"
  - "**/exec*.md"
  - "docs/exec-plans/**/*.md"
alwaysApply: false
---

# harness-plan-execution

> 完整设计：[HARNESS-SPEC](../../docs/design-docs/harness/harness-spec.md)

## step-0（必须通过才能进 Task 1）
1. 加载 HARNESS-SPEC，列出「已加载文档：xxx」
2. 创建 `docs/exec-plans/executing/{plan-id}-execute.md`
3. 摸底确认 Plan 可行

## 执行中
- 每完成一个 Task → 更新 execute 文件，记录状态和当前 Task
- 回复含「Plan 执行记录已更新：xxx，状态：xxx，当前 Task：xxx」
- 遇到阻塞 → 停止，写 execute 记原因，提示用户决策

## 通知（best-effort）
```bash
./scripts/notify_plan_status.sh "Harness Plan" "{plan-id} {事件}"
```
脚本不存在或失败仅记 execute，不阻塞执行。
